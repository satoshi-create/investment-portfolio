/** `investment_themes.name` / `structure_tags[0]` と一致させる（テーマ詳細 API の query）。 */
export const BITCOIN_STRUCTURAL_THEME_QUERY_NAME = "ビットコイン";

/** URL スラッグ `.../themes/bitcoin` から上記名へ解決する際に使用。 */
export const BITCOIN_STRUCTURAL_THEME_SLUG = "bitcoin";
