/** `holdings.earnings_summary_note` の最大文字数（API・UI で共通） */
export const EARNINGS_SUMMARY_NOTE_MAX_LEN = 8000;

/** `holdings.memo` / エコシステム `memo` の表示上の最大文字数（APIは未制限のため、長文の事故防止用） */
export const HOLDING_MEMO_MAX_LEN = 16000;
